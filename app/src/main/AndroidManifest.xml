<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFlashcard">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.TopicDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.ScanUploadActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.ProcessingActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.FlashcardStudyActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.QuizActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

        <activity
            android:name=".activities.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.SmartFlashcard.NoActionBar" />

    </application>

</manifest>